import { Button } from "../button/button";

const BetTicket = () => {
  return (
    <div className="container">
      <Button toggable={true} intent="primary"></Button>
      <Button> </Button>
      <Button> </Button>
    </div>
  );
};

export default BetTicket;
